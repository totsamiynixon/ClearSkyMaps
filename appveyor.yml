version: "1.0.{build}"
image: Visual Studio 2017
branches:
  only:
    - master
    - staging
    - production
for:
  - branches:
      only:
        - master
    build_script:
      - cmd: dotnet build server/ClearSkyMaps.sln
      - cmd: npm run build --prefix client/mobile-spa
  - branches:
      only:
        - production
    environment:
      ASPNETCORE_ENVIRONMENT: Production
      NOVE_ENV: production
    build_script:
      - cmd: dotnet publish server/ClearSkyMaps.sln -c Release -o ../../release
      - cmd: npm run build --prefix client/mobile-spa
      - cmd: xcopy client\mobile-spa\www release\wwwroot\spa /E /H
    artifacts:
      - path: ./release
        name: WebAppProd
        type: Zip
    deploy_script:
      - cmd: powershell -file deploy.ps1 -artifactPath ./server/release/WebAppProd.zip -domain {PRODUCTION_DOMAIN}
  - branches:
      only:
        - staging
    environment:
      ASPNETCORE_ENVIRONMENT: Staging
      NOVE_ENV: staging
    build_script:
      - cmd: dotnet publish server/ClearSkyMaps.sln -c Release -o ../../release
      - cmd: npm run build --prefix client/mobile-spa
      - cmd: xcopy client\mobile-spa\www release\wwwroot\spa /E /H
    artifacts:
      - path: ./release
        name: WebAppStaging
        type: Zip
    deploy_script:
      - cmd: powershell -file deploy.ps1 -artifactPath ./server/release/WebAppStaging.zip -domain {STAGING_DOMAIN}
init:
  # Good practise, because Windows line endings are different from Unix/Linux ones
  - cmd: git config --global core.autocrlf true
install:
  - cmd: dotnet restore ./server/ClearSkyMaps.sln
  - cmd: cd ./client/mobile-spa && npm install
clone_depth: 1
test:
  assemblies:
    - '**\*.Tests.dll'
